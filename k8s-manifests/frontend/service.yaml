apiVersion: v1
kind: Service
metadata:
  name: product-frontend
  labels:
    app: product-frontend
spec:
  type: ClusterIP
  selector:
    app: product-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
